<h2>Welcome to the kitty shop</h2>

<ul>
  <% @cats.each do |cat| %>
    <li>
      Name: <%= cat.name %>
      <%= link_to 'More Details', cat_url(cat) %>
    </li>
  <% end %>
</ul>

<%= link_to 'Add Cat', new_cat_url %>
